# RSI分层极值追踪量化交易系统依赖项
# 按功能分组，指定版本号以确保兼容性

# 1. FastAPI及其依赖组件
fastapi==0.103.1
uvicorn[standard]==0.23.2
pydantic==2.3.0
python-multipart==0.0.6
email-validator==2.0.0
python-dotenv==1.0.0
jinja2==3.1.2

# 2. 数据库相关
sqlalchemy==2.0.20
alembic==1.12.0
psycopg2-binary==2.9.7  # PostgreSQL驱动
asyncpg==0.28.0  # 异步PostgreSQL驱动
sqlalchemy-utils==0.41.1
greenlet==2.0.2  # SQLAlchemy异步支持

# 3. Redis相关
redis==4.6.0
hiredis==2.2.3  # C加速的Redis客户端

# 4. 交易所API客户端
ccxt==4.0.0  # 加密货币交易所统一API
httpx==0.24.1  # 异步HTTP客户端
websockets==11.0.3
aiohttp==3.8.5  # 异步HTTP客户端，某些交易所API需要

# 5. 技术指标计算
numpy==1.25.2
pandas==2.1.0
ta-lib==0.4.28  # 技术分析库，需要先安装系统依赖
pandas-ta==0.3.14b0  # 纯Python实现的技术分析库，备选
scipy==1.11.2

# 6. WebSocket支持
websockets==11.0.3
starlette==0.27.0  # FastAPI WebSocket支持

# 7. 任务队列
celery==5.3.4
flower==2.0.1  # Celery监控工具
kombu==5.3.2
billiard==4.1.0
amqp==5.1.1
vine==5.0.0

# 8. 日志和监控
loguru==0.7.0  # 更好的日志库
prometheus-client==0.17.1  # 指标监控
python-json-logger==2.0.7
sentry-sdk==1.30.0  # 错误跟踪

# 9. 加密和安全
cryptography==41.0.3
pyjwt==2.8.0
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0

# 10. 云存储和备份
boto3==1.28.40  # AWS S3
oss2==2.17.0  # 阿里云OSS
cos-python-sdk-v5==1.9.25  # 腾讯云COS
rclone-python==0.0.2  # rclone Python绑定
schedule==1.2.0  # 简单的任务调度库

# 11. 其他工具
pyyaml==6.0.1  # YAML配置文件支持
pytz==2023.3  # 时区支持
python-dateutil==2.8.2
ujson==5.8.0  # 更快的JSON处理
orjson==3.9.5  # 更快的JSON处理(FastAPI推荐)
tenacity==8.2.3  # 重试机制
cachetools==5.3.1  # 缓存工具
tqdm==4.66.1  # 进度条
aiofiles==23.2.1  # 异步文件操作
click==8.1.7  # 命令行工具
tabulate==0.9.0  # 表格输出
colorama==0.4.6  # 彩色终端输出
